<?xml version="1.0" encoding="utf-8"?>
<!-- Detail panel card, AMBIENT variant: same silhouette as am_detail_panel_bg but
     translucent, so the blurred cover art reads through the panel (desktop parity). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="#BF161C26" />
    <stroke android:width="1dp" android:color="#782E3947" />
    <corners android:radius="12dp" />
</shape>